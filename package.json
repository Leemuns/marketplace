{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server-marketplace\" \"npm run dev --prefix client-marketplace\"",
    "build": "npm install --prefix client-marketplace && npm run build --prefix client-marketplace && npm install --prefix server-marketplace",
    "start": "cross-env NODE_ENV=production npm start --prefix server-marketplace"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "@types/bcrypt": "^6.0.0",
    "bcrypt": "^6.0.0",
    "cross-env": "^10.1.0"
  }
}
